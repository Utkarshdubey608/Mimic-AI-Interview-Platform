fastapi==0.115.6
uvicorn[standard]==0.34.0
pydantic==2.10.4
pydantic-settings==2.7.1
email-validator==2.2.0
firebase-admin==6.6.0
google-api-python-client==2.156.0
httpx==0.28.1
python-multipart==0.0.20
# AWS Rekognition — facial analysis for the web screening track.
boto3==1.43.70
nh3==0.3.6
# Candidate extraction from uploaded files (web invite flow).
pypdf==6.16.0
python-docx==1.2.0
openpyxl==3.1.5
# LiveKit — the self-hosted two-way engine (rooms, tokens, egress, webhooks).
# Only needed when TWOWAY_ENGINE=livekit. Verified against 1.2.0 (room/egress
# request classes, AccessToken builder, WebhookReceiver(TokenVerifier(...))).
livekit-api==1.2.0
